# Shared dependencies for JarvisMail workers
# brain-worker, vision-worker, and notifier all use these

# Redis async client
redis>=5.0

# HTTP client (LLM calls, Telegram, Graph API)
httpx>=0.27

# Microsoft Authentication Library (Device Code Flow)
msal>=1.28


# LLM providers
google-genai>=1.0
groq>=0.12
ollama>=0.4
openai>=1.50

# Data validation
pydantic>=2.0

# Telegram bot (notifier)
python-telegram-bot>=21.0
